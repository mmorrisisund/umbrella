const LocationTitle = ({ location }) => {
  return (
    <section className='flex justify-center'>
      <h2 className='text-5xl text-blue-100'>{location}</h2>
    </section>
  )
}

export default LocationTitle
